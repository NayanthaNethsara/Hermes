# Environment
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/

# Data artifacts & corpus (raw archive, extracted images, database artifacts)
data/*
!data/**/.gitkeep
!data/.gitkeep
*.gpickle

# The Ashen Era Archive corpus itself - read-only source material
# (CLAUDE.md rule 2), too large/not ours to commit. Ingestion only reads
# it via --corpus-path; it doesn't need to live in the repo at all.
/Ashen_Era_Archive/

# Next.js
.next/
out/

# Logs and results
*.log
results/

# OS
.DS_Store
Thumbs.db
