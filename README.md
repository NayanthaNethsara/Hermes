# The Archivist

An AI research assistant that answers questions over the Ashen Era Archive by searching the way a human would — planning searches, weighing source trust, and surfacing contradictions instead of hiding them.

## Setup

See `configuration-example/.env.example` — copy it to `.env` in the repo root and fill in your keys.
