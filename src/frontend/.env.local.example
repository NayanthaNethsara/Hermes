# Base URL of the FastAPI backend (src/backend/main.py). Copy this file
# to .env.local and adjust if the backend runs somewhere other than the
# default local port.
NEXT_PUBLIC_API_URL=http://localhost:8000
